unsetopt correct_all
setopt correct

alias mv='mv -i'
alias ls='ls --color=auto'
#alias icat='kitten icat'
#alias s='kitten ssh'
alias s='wezterm ssh'

# https://github.com/nvbn/thefuck
eval $(thefuck --alias)

#mysettitle() {
#  printf "\033k$1\033\\"
#  #printf "\033]0;$1\a"
#}

#ssh() {
#  mysettitle "ssh:$*"
#  command ssh "$@"
#  mysettitle "zsh"
#}

#mosh() {
#  mysettitle "mosh:$*"
#  command mosh "$@"
#  mysettitle "zsh"
#}

# lazygit https://github.com/jesseduffield/lazygit
lg() {
  export LAZYGIT_NEW_DIR_FILE=~/.lazygit/newdir

  lazygit "$@"

  if [ -f $LAZYGIT_NEW_DIR_FILE ]; then
    cd "$(cat $LAZYGIT_NEW_DIR_FILE)"
	rm -f $LAZYGIT_NEW_DIR_FILE > /dev/null
  fi
}

# lazydocker https://github.com/jesseduffield/lazydocker
lzd() {
	lazydocker "$@"
}

[ -s "/home/longsleep/.scm_breeze/scm_breeze.sh" ] && source "/home/longsleep/.scm_breeze/scm_breeze.sh"

# fzf https://github.com/junegunn/fzf
source <(fzf --zsh)

# broot https://dystroy.org/broot/
source /home/longsleep/.config/broot/launcher/bash/br
