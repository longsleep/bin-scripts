---
title: 'Git Commit'
read_only: true
type: 'command'
---

# Create new git commit

Use the git-commit-enhancer agent to create a new git commit with improved commit messages.

**Default behavior**: Requires staged changes before creating commit.

**With `--auto` flag**: Automatically stages appropriate files using `git add`, then prompts user for confirmation before committing the selected changes.
